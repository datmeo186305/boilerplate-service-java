server:
  port : 8081

eureka:
   client:
      healthcheck:
         enabled: true   
         
management:
   endpoints:
      web:
         exposure:
            include: "*"
      health:
         show-details: always
      hystrix:
         stream:
            enabled: true

hystrix:
   dashboard:
      proxy-stream-allow-list: "*"         